<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.finki.timska.magarec"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="22" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".GameWindow"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:screenOrientation="landscape"
            android:theme="@style/FullscreenTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AboutActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_about"
            android:parentActivityName=".GameWindow"
            android:screenOrientation="landscape"
            android:theme="@style/FullscreenTheme" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.finki.timska.magarec.GameWindow" />
        </activity>
        <activity
            android:name=".HowToActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_how_to"
            android:parentActivityName=".GameWindow"
            android:screenOrientation="landscape"
            android:theme="@style/FullscreenTheme" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.finki.timska.magarec.GameWindow" />
        </activity>
        <activity
            android:name=".NewGameActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_new_game"
            android:parentActivityName=".GameWindow"
            android:screenOrientation="landscape"
            android:theme="@style/FullscreenTheme" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.finki.timska.magarec.GameWindow" />
        </activity>
        <activity
            android:name=".PlayerTimeActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_player_time"
            android:parentActivityName=".NewGameActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.Transparent" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.finki.timska.magarec.NewGameActivity" />
        </activity>
    </application>

</manifest>
